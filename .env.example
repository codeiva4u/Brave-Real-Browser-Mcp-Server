# Brave Browser Path (Optional - Auto-detected if not set)
# Windows Example: C:\Program Files\BraveSoftware\Brave-Browser\Application\brave.exe
# Mac Example: /Applications/Brave Browser.app/Contents/MacOS/Brave Browser
# Linux Example: /usr/bin/brave-browser
BRAVE_PATH=

# Headless Mode (true/false)
# PRIORITY: ENV variable ALWAYS wins over code/API settings
# Set to 'true' for background mode (no visible browser)
# Set to 'false' to see the browser window (Recommended for debugging)
# If not set, defaults to false (GUI visible)
HEADLESS=false

# Disable Xvfb Virtual Display (Linux only)
# PRIORITY: ENV variable ALWAYS wins over code/API settings
# Set to 'true' to disable Xvfb (recommended for most cases)
# Set to 'false' to use Xvfb virtual display
# If not set, defaults to true (Xvfb disabled)
DISABLE_XVFB=true

# Content Priority
# Set to 'true' to disable content prioritization (for testing)
DISABLE_CONTENT_PRIORITY=false

# Proxy Configuration (Optional)
# PRIORITY: ENV variable ALWAYS wins over code/API settings
# Format: protocol://host:port (e.g., http://127.0.0.1:8080)
# Leave empty to disable proxy
PROXY_URL=

# Screen Recorder
# Set to 'true' to enable screen recording capabilities
ENABLE_RECORDING=true
